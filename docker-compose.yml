version: "3.9"

services:
  api:
    build: .
    container_name: rag-chatbot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount data folder so uploaded documents persist outside the container
      - ./data:/app/data
    restart: unless-stopped